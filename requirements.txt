fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-jose[cryptography]>=3.3.0
# Directa, no transitiva: app/storage/crypto.py importa cryptography.fernet para
# cifrar las API keys de proveedor. Llegaba solo como extra de python-jose, así
# que quitar ese extra rompía el cifrado sin que nada lo avisara.
cryptography>=42.0.0
bcrypt>=4.0.0
python-multipart>=0.0.9
pyyaml>=6.0.1
httpx>=0.27.0
pypdf>=4.0.0
Pillow>=10.4.0
pillow-heif>=0.18.0
pytesseract>=0.3.13
asyncpg>=0.29.0
aiosqlite>=0.20.0
stripe>=10.0.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
# pytest.ini declara `timeout = 30` y rtests.py pasa --timeout=30. Sin este
# paquete pytest avisa "Unknown config option: timeout", ignora el límite en
# silencio, y rtests.py aborta con "unrecognized arguments" en entorno limpio.
pytest-timeout>=2.3.0
